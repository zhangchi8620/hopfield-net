#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <algorithm>
#include <cmath>

using namespace std;

int generatePatt();
int imprintPatt(int);
int testPatt(int);
int *genRand(int);
bool updateNetwork(int);
